## Usage

```
ln -s ~/dotfiles/zsh/.zshrc ~/.zshrc
zsh  # maybe with proxychains4
```

## Requirements

- base: zsh, git, ncurses-utils (zinit may use tput)
- opt: svn, zoxide, fzf, eza, bat, trash-cli
- laptop: upower
